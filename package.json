{
  "name": "laundry-assist",
  "version": "1.0.0",
  "scripts": {
    "start": "python -u -m streamlit run streamlit_app.py --server.port 8501",
    "dev": "python -u -m uvicorn api:app --port 8000 --reload",
    "api": "python -u run_server.py"
  }
}
